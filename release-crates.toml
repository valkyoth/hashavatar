[release]
version = "2.0.0-alpha.4"
publish_order = ["hashavatar-core", "hashavatar"]

[crates.hashavatar-core]
previous_version = "0.1.0-alpha.3"
version = "0.1.0-alpha.4"
change = "code"
publish = false
reason = "Add bounded typed layered-style resolution and canonical layer compilers; all prerelease packages remain source-only."

[crates.hashavatar]
previous_version = "2.0.0-alpha.3"
version = "2.0.0-alpha.4"
change = "code"
publish = false
reason = "Expose alpha.4 accessory, expression, palette, resolved-style, and layout-report APIs; prerelease publication is prohibited."
