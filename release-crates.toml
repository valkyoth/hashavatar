[release]
version = "2.0.0-alpha.2"
publish_order = ["hashavatar-core", "hashavatar"]

[crates.hashavatar-core]
previous_version = "0.1.0-alpha.1"
version = "0.1.0-alpha.2"
change = "code"
publish = false
reason = "Complete bounded canonical renderer contracts; all prerelease packages remain source-only."

[crates.hashavatar]
previous_version = "2.0.0-alpha.1"
version = "2.0.0-alpha.2"
change = "code"
publish = false
reason = "Expose validated surface, pixel digest, and SVG option contracts; prerelease publication is prohibited."
